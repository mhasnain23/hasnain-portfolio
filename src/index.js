const typed = new Typed('#element', {
    strings: ['Web Developer', 'UI UX Designer', 'Frontend Developer'],
    typeSpeed: 50,
    backSpeed: 30,
    backDelay: 1000,
    loop: true,
})

let userInfo = document.querySelectorAll("send");